Django==4.2.7
djangorestframework==3.14.0
django-cors-headers==4.3.1
channels==4.0.0
daphne==4.0.0
Pillow==10.1.0
python-decouple==3.8
anthropic==0.25.0
whitenoise==6.6.0
gunicorn==21.2.0
django-widget-tweaks==1.5.0
dj-database-url==2.1.0
# psycopg2 NOT needed locally — we use SQLite
# Install this only if you switch to PostgreSQL locally:
# psycopg2-binary
